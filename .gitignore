# Exclude frontend, backend, and mm-squad-trips repos
frontend/
backend/
mm-squad-trips/

# Per-machine Claude Code permissions (diverges across devices, never share)
.claude/settings.local.json

# Exclude lovable_pages sub-repos (each folder inside is its own git repo)
lovable_pages/*/

# Common ignores
node_modules/
nul
.env
.env.local
*.log
.DS_Store

# Ad-hoc Playwright verification scripts/screenshots (superseded by frontend/e2e/)
playwright-verify/